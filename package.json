{
  "name": "manipulated-image-detector-extension",
  "version": "1.0.0",
  "description": "Predict whether an image has been manipulated.",
  "main": "background.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Nicholas Gin",
  "license": "ISC",
  "dependencies": {
    "@tensorflow/tfjs": "^4.6.0"
  }
}
